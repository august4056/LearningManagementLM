{
  "version": 2,
  "framework": "vue",
  "buildCommand": "npm ci && npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "env": {
    "VUE_APP_API_BASE_URL": "@api-base-url"
  }
}
